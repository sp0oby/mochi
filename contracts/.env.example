# Mochi Garden deploy env — copy to .env and fill in.
# .env is gitignored. Never commit a real PRIVATE_KEY.

# Deployer wallet — must hold Base Sepolia ETH (faucet OK)
PRIVATE_KEY=0x_YOUR_PRIVATE_KEY_HERE

# Optional: where the 1% protocol fee on buys/mints lands.
# Defaults to the deployer if unset.
# DEV_TREASURY=0x_YOUR_TREASURY_HERE

# Base Sepolia v4 PoolManager (verified onchain Mar 2026)
POOL_MANAGER=0x05E73354cFDd6745C338b50BcFDfA3Aa6fA03408

# RPC for `--rpc-url base_sepolia` (or pass directly to forge script)
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org

# Etherscan v2 multichain API key, used by `forge verify-contract`
# BASESCAN_API_KEY=YOUR_KEY
